[pytest]
# Only collect the real test suite. This deliberately excludes the repo-root
# scripts test_binance_testnet.py (a manual connection check that `return`s a
# bool instead of asserting) and backtest_example.py, which are runnable tools,
# not unit tests.
testpaths = tests
